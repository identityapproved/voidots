#!/bin/sh
out="$(wpctl get-volume @DEFAULT_AUDIO_SINK@ 2>/dev/null)" || { printf "vol?"; exit; }
case "$out" in
    *MUTED*) printf "󰝟 mute" ;;
    *) printf " %d%%" "$(printf '%s' "$out" | awk '{printf "%d", $2*100}')" ;;
esac
